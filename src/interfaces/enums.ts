

export enum vartype {
    text = "text",
    number = "number",
    email = "email",
    default = "default"
}

export enum reqtype {
    default = "default",
    optional = "optional",
    required = "required"
}

